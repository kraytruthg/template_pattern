require "spec_helper"

describe TemplatePattern do
  it "has a version number" do
    expect(TemplatePattern::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
